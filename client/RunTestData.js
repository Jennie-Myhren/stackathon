import React from 'react';
import { accuracy } from '../brain/neuralNet';

const RunTestData = () => {
  ///WORK ON WHAT YOU WANT TO INPUT; WILL HAVE A TOGGLE BUTTON (RUN TEST DATA)
  // const computerResults = () => trainedBrain.run(cleanTestData);

  return <div>{accuracy}</div>;
};

export default RunTestData;
